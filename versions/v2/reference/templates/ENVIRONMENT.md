# Environment Cache — [PROJECT NAME]

Generated: [YYYY-MM-DD] by full onboarding (CORE_PROTOCOLS.md §4.2). Delete this file to force re-onboarding.

- **Project root**: [absolute path]
- **Agent host shell**: [shell + version that the agent's command tool executes — may differ from the machine default]
- **Chaining operator**: [`&&` for bash | `;` for PowerShell]
- **Build command**: [command or "none"]
- **Test command**: [command or "none"]
- **Machine quirks / exceptions**: [e.g., tool X only via `wsl -d Ubuntu -- ...`, curl needs `--ssl-no-revoke`, env var `Y=1` required for tool Z]
- **Source mtimes (optional)**: [`AGENTS.md`: mtime, ...]
